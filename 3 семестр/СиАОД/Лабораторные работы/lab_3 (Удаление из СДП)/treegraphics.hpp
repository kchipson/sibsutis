#ifndef TREEGRAPHICS_HPP
#define TREEGRAPHICS_HPP

#include "struct.hpp"

void treeGraphics(tree *p, unsigned int heightLevels = 500,
                  float zoomAmount = 1.5F, float sizeBlock = 1.0F);

#endif